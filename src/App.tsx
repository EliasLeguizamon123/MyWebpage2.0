import Router from './routes/Router';
import { Box } from '@chakra-ui/react';

function App() {
    return (
        <Box h={'full'} w={'full'}>
            <Router />
        </Box>
    );
}

export default App;
